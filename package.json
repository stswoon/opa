{
  "name": "opa-boilerplate",
  "version": "0.0.4",
  "description": "One Page App boilerplate",
  "engines": {
    "node": "14.x"
  },
  "license": "MIT",
  "scripts": {
    "build": "npm run server:ci-and-build && npm run client:ci-and-build && npm run client:copy && npm run patch-version",
    "start": "cd server && npm run start",
    "-----": "echo 0",
    "server:ci-and-build": "cd server && npm ci && npm run build",
    "client:ci-and-build": "cd client && npm ci && npm run build",
    "client:copy": "cd server && npm run copyClientDist",
    "patch-version": "npm --no-git-tag-version version patch"
  }
}
